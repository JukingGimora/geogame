# 进展与待办（截至 2026-09-26 凌晨）

> 这份文件记的是「现在到哪了、接下来做什么、哪些事已经定死别再翻」。
> 每次开工先看它，别再从记忆里重新讨论一遍。

## 一、已经在线上跑着（后端，重启即生效，无需提审）

| 事项 | 说明 |
|---|---|
| 题库 205 张 | 66 已上线 + 138 待审 + 1 拒绝；23 个国家、7 个文化圈有货 |
| 文化圈归属 | 162 国中心点 + 160 城市锚点，最近邻判定；边境、中亚、马来半岛已补密 |
| 提示阶梯 | ③ 文化圈 → ④ 国家（大国加方位，如「中国·西北部」）。一套规则，无例外 |
| AI 两套输出 | 猜前给「该怎么推」（禁地名）；猜后给结论+依据（可点名）；坐标由城市名查表定位 |
| 三条命 + 连关 | 超 800km 掉一条，掉光或题库走空结束；排行榜主榜「最长连关」 |
| 有故事的昵称 | 1920 种组合，280 个「旅行者」已改名 |
| 上传读 GPS | 照片自带坐标就不让用户找地图，读不到才出现地图 |
| 批量导入 | `tools/import_photos.py`，从 EXIF 读坐标，重复自动跳过 |
| 审核页 | 能写故事、显示国家/文化圈/最近城市（核对坐标用） |
| 邮件提醒 | 每小时检查，有待审就发到 862722762@qq.com，没有不发 |
| 安全 | 按 IP 限速、每账号每天 20 张上传上限 |
| 修掉的线上 bug | 游客登录并发 500、AI 提示念石碑泄底、境外照片被标成中国省份、AI 提示词写死「猜中国」 |
| 运维 | 免密重启（sudoers 只放开 geogame-backend） |

## 二、改完了但**没上线**（等提审小程序）

- 首页换成**世界地图**：九个文化圈按参考图配色、画边界线、标名字，按进度分四档亮度；点地图或图例选圈
- **拖动十字插旗**（修「点了没反应」——微信地图点到地名时触发的是另一个事件）
- **选图接口修复**（`chooseImage` → `chooseMedia`，补失败提示；修「上传没反应」）
- **合规四件**：不再用微信昵称/头像能力、像素头像、注销账号、删掉点不开的「同意」勾选框
- 开场页看完**直接进第一关**，不再经过首页
- 红心 ♥♥♡ + 连过 N 关、揭晓页显示「拍摄于 XX 附近」、提示按钮改名「大致范围 / 再缩小点」
- 埋点补齐：开场、首页按钮、首次落点、提交猜测、上传每一步、排行榜页签、点文化圈

## 三、还没做

**机制**
- [ ] **掉血的动画和音效**（现在只有震动，心从 ♥ 变 ♡ 没有动效；音效默认关闭的开关也还没做）
- [ ] AI 减配：现在太准（布哈拉差 0.58km），玩家赢不了。要不要只让它看裁剪/压缩过的图
- [ ] 认真模式的解锁规则（在某圈猜过 10 张或 60% 才能解锁相邻圈）——只讨论过，未实现
- [ ] 漫游 3 关 / 认真模式无限闯关的区分——未实现，目前只有一套

**内容与运营**
- [ ] **审核那 138 张**（顺手写故事）→ 通过后地图上另外六个圈才会亮
- [ ] 13 张被旧限速挡掉的照片可以补跑；5 张无 GPS 的要手动补坐标
- [ ] 130 多张境外照片的 AI 结果是旧提示词算的，建议重算（约一毛五）

**合规**
- [ ] 隐私指引补一句：会读取所选照片中记录的拍摄位置
- [ ] 昵称的内容安全检查（小程序走微信接口，H5 走词表或阿里云）
- [ ] 提审小程序

**其他**
- [ ] 世界地图的圈边界线还很糙，需要再调几轮
- [ ] 英文版 H5（界面翻译 + 故事精翻 + 国外访问速度）
- [ ] logo 保持背包不变，等整体定型后再出新版

## 四、已经定死的决策（别再翻）

1. **AI 的三个角色**：猜前给角度（禁地名）／猜后给答案（可点名）／后台给你核对坐标（要准）
2. **提示只有两级**：大范围 → 小范围，中外同一套规则，绝不到城市
3. **九大文化圈**按人文地理那张图划分，配色按亨廷顿那张图
4. **配色**：暖褐夜色，只在 `App.vue` 的 CSS 变量和 `lib/theme.ts` 两处定义
5. **照片归属**：全部挂在 Juking（17）名下，不转给别人
6. **命**：三条，800km 掉一条
7. **H5 与小程序**：同一套后端、同一个榜，差别只在语言、入口、身份找回、分享
